{
  "name": "moyo-wasm",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "build": "wasm-pack build --target web --release"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
